
/* === Nav active highlight === */
header nav a[data-active="true"],
header nav a[aria-current="page"]{
  background: rgba(255,255,255,.12);
  text-decoration: none;
  border-radius: 10px;
}

/* === Subtle entrances on scroll (cards, sections) === */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(12px); }
  to   { opacity: 1; transform: translateY(0); }
}
@media (prefers-reduced-motion: no-preference){
  #projects article,
  #services li,
  #about .container,
  #contact form{
    animation: fadeUp .6s ease both;
  }
  #projects article:nth-child(1){ animation-delay: .05s }
  #projects article:nth-child(2){ animation-delay: .15s }
  #projects article:nth-child(3){ animation-delay: .25s }
}

/* Hover lift for cards */
#projects article:hover, #services li:hover{
  transform: translateY(-2px);
  transition: transform .18s ease;
}
